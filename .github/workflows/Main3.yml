# .github/workflows/main.yml
name: Deploy to Spaces

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Deploy to Hugging Face
      run: |
        echo "Auto-deploying to Hugging Face Spaces..."
        # Spaces automatically deploys on push to main
